# Solution www.deep-ml.com problems
Trying not to use installed libraries (unless an assignment says otherwise)

## Structure
* `daily_problems/` - module contains files with solutions for daily issues (will be moved to other modules)
* `'category_name'/` - module contains files with solutions for issues with relevant category (see relevant directory's readme)

Filename for files in modules (except daily problems): `taskNumber_difficulty.py`
